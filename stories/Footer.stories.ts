import { Footer } from "../components/Common/Footer";

const meta = {
  title: "Layout/Footer",
  component: Footer,
};

export default meta;

export const Default = {};

export const Mobile = {
  parameters: {
    viewport: {
      defaultViewport: "mobile1",
    },
  },
};
