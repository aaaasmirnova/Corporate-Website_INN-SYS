import { Footer } from "../components/Footer";

const meta = {
  title: "Layout/Footer",
  component: Footer,
};

export default meta;

export const Default = {};

export const Mobile = {
  parameters: {
    viewport: {
      defaultViewport: "mobile1",
    },
  },
};
